// Se a página contém um formulário, verifica se o formulário está válido
// se possui um botão de submit, verifica se o botão está válido
// se possui botao sim e nao, verifica se o botão está válido
// se somente um dado pode ser selecionado no botao de cada pergunt
// se possui 4 perguntas
// se possui um campo select
// se o campo select possui os valores sim, nao, nao sei e agora
// se somente um valor pode ser selecionado no campo select
// se possui um campo textarea
// se o campo textarea limita o mínimo e máximo de caracteres
// se possui um campo contador de caracteres
// se o campo contador de caracteres está limitado ao máximo de caracteres e mínimo
// se ao clicar no botão de submit, o formulário é enviado
// se ao enviar formulario aparece o campo resultado
// se o campo resultado possui total de respostas, quantidae de respostas nao, quantidade de respostas sim e quantidade de respostas nao sei
// se o campo resultado possui porcentagem de positivas, negativas e neutras
